# Process config example for dataset

# global parameters
project_name: 'belle_data0.5M_cn'
dataset_path: './input/belle_data0.5M_cn.jsonl'  # path to your dataset directory or file
np: 4  # number of subprocess to process your dataset

export_path: './output/analysis/belle_data0.5M_cn/belle_data0.5M_cn.jsonl'

# process schedule
# a list of several process operators with their arguments
process:
  - math_num_filter:
      min_len: 1
      
