# Process config example for dataset

# global parameters
project_name: 'GPTeacher'
dataset_path: './input/GPTeacher.jsonl'  # path to your dataset directory or file
np: 4  # number of subprocess to process your dataset

export_path: './output/analysis/GPTeacher/GPTeacher.jsonl'

# process schedule
# a list of several process operators with their arguments
process:
  - math_num_filter:
      min_len: 1
      

