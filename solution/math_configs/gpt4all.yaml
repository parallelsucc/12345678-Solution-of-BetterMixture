# Process config example for dataset

# global parameters
project_name: 'gpt4all'
dataset_path: './input/gpt4all.jsonl'  # path to your dataset directory or file
np: 4  # number of subprocess to process your dataset

export_path: './output/analysis/gpt4all/gpt4all.jsonl'

# process schedule
# a list of several process operators with their arguments
process:
  - math_num_filter:
      min_len: 1
      
