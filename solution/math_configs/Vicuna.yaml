# Process config example for dataset

# global parameters
project_name: 'Vicuna'
dataset_path: './input/Vicuna.jsonl'  # path to your dataset directory or file
np: 4  # number of subprocess to process your dataset

export_path: './output/analysis/Vicuna/Vicuna.jsonl'

# process schedule
# a list of several process operators with their arguments
process:
  - math_num_filter:
      min_len: 1
      
